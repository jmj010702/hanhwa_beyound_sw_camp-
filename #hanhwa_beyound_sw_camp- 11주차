# BEYOND SW 캠프 23기 — 10주차 회고

##📚 Findings — 무엇을 배웠나?
n+1, 필터링 예외처리, 미들웨어, csrf, AT토큰, RT토큰, was vs web, yml파일 분리,
페이징 처리(Page클래스를커스텀하고자 한다면 너무 복잡해지기에 비추천)
검색 객체(Specification)
검색+페이징은 한 세트로 쓰인다

예약 글쓰기 
-> 글쓸 때 open시각 정하는 것
-> 12시에 open하겠다
-> Appoinment,, AppoimentTime
->예약안함(즉시)N, 예약함(나중)Y 12:00
-> 스케쥴러 : 자동으로 실행되는 프로그램 : Y인건을 찾아 현재시간하고 비교. -> 지났으면 N으로 변경, 안 지났으면 pass
-> 조회조건 N 
->스케쥴러의 기준점 : 년,월,일,시,분,초, 클론 

rt,at는 redis로 저장 
batch : 대량의 데이터를 일정한 규칙에 따라 자동으로 처리하는 프로세스(작업의 리스트만 정하는것 그렇기에 해당 작업을 시작하기 위해서는 
스케쥴러를 통해서 트리거를 작동해야함)

배치는 복잡한 연산을 순차적으로 해야 할 때 사용 
 배치를 사용하려면 기존에 디비가 미리 있어야함 

servlet(서블릿) : http 요청/응답을 쉽게 해주는 Spring 기술 
-> 더 발전된 형태 -> Controller를 통해서 조금 더 쉽게 

Aop : 관점 지향 프로그래밍이라 불림, 주된 목적은 공통 사용코드와 핵심 관심사항등 특정 관점을 가지고 코드를 분리하는 것
Common 폴더와 비슷함/ 어느 계층에서든 사용할 수 있고 계층과 계층 사이에 aop를 짜서 가는 길목에 인터셉트 할 수 있음. 

프록시 : aop는 프록시 객체를 사용하여 동작/ 실체 객체의 메서드 호출을 가로채는 대리 객체 
클라이언트는 프록시를 통해 메서드를 호출하고, 프록시는 부가 로직을 실행한 뒤 실제 객체의 메서드를 호출

aws s3를 이용한 이미지 올리기 
IAM 계정 만들기 
-> 사용자-> 사용자 생성 -> 권한(S3FullAccess) -> 사용자 생성 
액세스키 = 프로그램에서 접근할 때 사용하는 키








## 🧠 Feelings — 어떻게 느꼈나?

## 🔮 Future — 다음에 어떻게 활용할까?


## ✏️ 마무리 한 줄
